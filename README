Quick Start
1. Download python libraries by running `pip install -r requirements.txt`
2. Download spaCy model by running `python - spacy download en`
3. Download nltk models as follows:
	nltk.download('punkt') # first-time use only
	nltk.download('wordnet') # first-time use only
	nltk.download('maxent_ne_chunker')
	nltk.download('words')
	nltk.download("treebank")
	nltk.download("averaged_perceptron_tagger")
4. Run program with `python main.py`
5. (Optional) Run directory with `python main.py --dir (directory_name)`
